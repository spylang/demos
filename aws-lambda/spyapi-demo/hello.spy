from spyapi import SPyAPI

app = SPyAPI()

@app.get("/")
def index() -> None:
    app.response(200, '{"message": "Hello from SPy!"}', 'application/json')


@app.get("/login")
def login() -> None:
    user: str = app.get_query_param("user")
    app.response(200, f'{{"message": "Hello from login, user={user}"}}', 'application/json')


def main() -> None:
    app.serve_forever()
