[package]
name = "rust-demo"
version = "0.1.0"
edition = "2021"

# cargo-lambda looks for a binary named "bootstrap"
[[bin]]
name = "bootstrap"
path = "src/main.rs"

[dependencies]
lambda_http = "0.13"
tokio = { version = "1", features = ["macros"] }

[profile.release]
lto = true
codegen-units = 1
